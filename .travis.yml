language: php

php:
  - 7.0
  - 7.1

sudo: false

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction --prefer-dist
  - php artisan key:generate

script:
  - vendor/bin/phpunit